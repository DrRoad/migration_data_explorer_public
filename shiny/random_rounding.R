## This is the public version of the random_rounding code.
## The rounding function doesn't do anything, as the data
##  for the public version is pre-rounded.

do_round = function(x, seed) x
specify_seed = function(env) NA
